// @todo Implement proper serializers for the models.

// eslint-disable-next-line no-unused-vars
function serialize(model, scheme) {
  return model;
}

// eslint-disable-next-line no-unused-vars
function serializeAll(models, scheme) {
  return models;
}

module.exports = { serialize, serializeAll };
